{
  "name": "@browxai/capability-testbed",
  "version": "0.0.0",
  "private": true,
  "description": "Agentic capability test app + harness for browxai. NOT part of CI — driven on-demand by the agentic test-suite workflow (Full Report > Diagnose > Fix). Serves a multi-surface web app that exercises every browser capability, and a harness that drives all 198 browxai tools against it.",
  "author": "Kalebtec",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "serve": "tsx src/server/main.ts",
    "report": "tsx src/harness/run-report.ts",
    "dogfood": "pnpm run dogfood:build && node dogfood/dist/dogfood/src/haiku.js",
    "dogfood:build": "tsc -p dogfood/tsconfig.build.json",
    "dogfood:typecheck": "tsc -p dogfood/tsconfig.json --noEmit",
    "dogfood:test": "pnpm run dogfood:build && node --test dogfood/dist/dogfood/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "browxai": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
