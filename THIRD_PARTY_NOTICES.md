# Third-party notices

browxai's runtime dependencies (production-only):

- `@modelcontextprotocol/sdk` — MIT
- `playwright-core` — Apache-2.0
- `zod` — MIT

This file is regenerated on every production-dependency change. Generation
runs as part of CI; manual: `pnpm licenses:notices` once the devDep is in
place. Full license texts for each dependency are bundled in the published
npm package under each package's own `node_modules/<name>/LICENSE`.

A complete generated `THIRD_PARTY_NOTICES.md` with verbatim license texts
will replace this stub before v1.0.0 cuts (Phase 15 deliverable).
