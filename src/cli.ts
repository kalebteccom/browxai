#!/usr/bin/env node
// browxai CLI entry — placeholder. Will start the MCP server (stdio transport).
import { NAME, VERSION } from "./index.js";

console.error(`${NAME} ${VERSION} — not implemented yet (Phase 0). See PHASE-0.md.`);
process.exit(0);
