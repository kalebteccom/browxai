node_modules/
dist/
dist-spike/
*.tsbuildinfo
coverage/
# spike + production transient state — lives in $BROWX_WORKSPACE (~/.browxai/), NOT here.
# Anything that does land in-tree under these legacy names is still ignored as a guardrail.
.browx-spike-profile/
spike/runs/
.env
.env.*
# captured / managed browser session state — never commit
.auth/
.browx-profile/
*.storageState.json
# editor / OS cruft
.DS_Store
.idea/
.vscode/
*.log
