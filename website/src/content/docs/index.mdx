---
title: The same tools for a web page and a native app
description: One MCP tool surface over eight engines in three families. Five browsers, including real Safari and real Chrome on an Android device, two native-app engines on an Android emulator and the iOS Simulator, and a desktop Electron app attached over CDP, plus the OS clipboard, password managers and a workspace directory.
template: splash
tableOfContents: false
editUrl: false
lastUpdated: false
---

<div class="browx-hero not-content">
  <div class="browx-hero-copy">
    <p class="browx-kicker">MCP-native, across eight engines and onto the host</p>
    <h1 class="browx-wordmark">brow<span class="x">x</span>ai</h1>
    <div class="browx-lede">
      The same tools for a web page and a native app. Eight engines in three
      families: five browsers, two native apps on an emulator or simulator, and
      a desktop Electron app. Everything past a browser starts off.
    </div>
    <div class="browx-cta">
      <a href="/getting-started/" class="browx-btn browx-btn--primary">Get started</a>
      <a href="/reference/tool-reference/" class="browx-btn browx-btn--ghost">Tool reference</a>
    </div>
    <div class="browx-install">
      <span class="pfx">$</span> npm install -g browxai
    </div>
  </div>

  <div class="browx-window" aria-hidden="true">
    <div class="browx-window-bar">
      <span class="browx-window-dots"><i></i><i></i><i></i></span>
      <span class="browx-window-title">agent <span class="dot">·</span> snapshot()</span>
    </div>
    <div class="browx-snap">
      <div class="browx-snap-note"># a page an agent can read, not a DOM dump</div>
      <div class="row">
        <span class="t-role">form</span>
        <span class="t-str">"Sign in"</span>
        <span class="t-acc">[ref=e3]</span>
        <span></span>
      </div>
      <div class="row indent">
        <span class="t-role">textbox</span>
        <span class="t-str">"Email"</span>
        <span class="t-acc">[ref=e4]</span>
        <span class="t-acc">actionable</span>
      </div>
      <div class="row indent">
        <span class="t-role">textbox</span>
        <span class="t-str">"Password"</span>
        <span class="t-acc">[ref=e5]</span>
        <span class="t-acc">actionable</span>
      </div>
      <div class="row indent">
        <span class="t-role">button</span>
        <span class="t-str">"Continue"</span>
        <span class="t-acc">[ref=e6]</span>
        <span class="t-acc">actionable</span>
      </div>
    </div>
  </div>
</div>

<div class="browx-diagram not-content">
  <div
    class="browx-loop"
    role="img"
    aria-label="The agent loop runs as a cycle: navigate to a URL, snapshot the page into refs, find the target, act on it, then the ActionResult feeds the next pass. The four steps repeat."
  >
    <svg class="browx-loop-arcs" viewBox="0 0 320 320" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
      <defs>
        <marker id="bx-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6.5" markerHeight="6.5" orient="auto">
          <path d="M0 0 L10 5 L0 10 z" fill="var(--sl-color-text-accent)" />
        </marker>
        <linearGradient id="bx-ring" x1="0" y1="0" x2="1" y2="1">
          <stop offset="0" stop-color="var(--browx-aurora-bright)" />
          <stop offset="1" stop-color="var(--browx-violet-600)" />
        </linearGradient>
      </defs>
      <circle class="bx-ring-glow" cx="160" cy="160" r="118" />
      <path class="bx-arc" d="M196 54 A118 118 0 0 1 266 124" marker-end="url(#bx-arrow)" />
      <path class="bx-arc" d="M266 196 A118 118 0 0 1 196 266" marker-end="url(#bx-arrow)" />
      <path class="bx-arc" d="M124 266 A118 118 0 0 1 54 196" marker-end="url(#bx-arrow)" />
      <path class="bx-arc" d="M54 124 A118 118 0 0 1 124 54" marker-end="url(#bx-arrow)" />
    </svg>

    <span class="bx-hub" aria-hidden="true">ActionResult<small>closes the loop</small></span>

    <div class="bx-step bx-step--n" aria-hidden="true">
      <span class="bx-step-k">navigate</span>
      <span class="bx-step-d">go to a URL</span>
    </div>
    <div class="bx-step bx-step--e" aria-hidden="true">
      <span class="bx-step-k">snapshot</span>
      <span class="bx-step-d">read the page as refs</span>
    </div>
    <div class="bx-step bx-step--s" aria-hidden="true">
      <span class="bx-step-k">find</span>
      <span class="bx-step-d">locate a target</span>
    </div>
    <div class="bx-step bx-step--w" aria-hidden="true">
      <span class="bx-step-k">act</span>
      <span class="bx-step-d">click, fill, verify</span>
    </div>

  </div>
</div>

<div class="browx-section not-content">
  <h2 class="browx-section-title">Why it exists</h2>
  <div class="browx-section-sub">
    browxai is not a wrapper over someone else's MCP server. It owns its own
    transport to all eight engines it drives, so it owns the whole session
    lifecycle. Where a task leaves the browser, for a native app, a desktop app
    or your host machine, browxai follows it under a capability you switch on.
  </div>

  <div class="browx-grid">
    <div class="browx-card">
      <span class="browx-card-mark">// surface</span>
      <h3>Token-efficient</h3>
      <div class="browx-card-text">
        <code>snapshot()</code> is a scoped, paginated, budgeted accessibility
        tree. <code>find()</code> returns ranked candidates with evidence, not a
        guess. Results stay small on purpose.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// native</span>
      <h3>Native apps, same tools</h3>
      <div class="browx-card-text">
        Switch on <code>native-device</code> and <code>find</code>,
        <code>click</code> and <code>fill</code> drive an app on an Android
        emulator or the iOS Simulator. The tree comes off the app's view
        hierarchy, with the same refs a page gets. Android is verified against a
        live emulator; the iOS lane is newer and needs a WebDriverAgent you run.
        Emulators and simulators only, no real devices.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// desktop</span>
      <h3>The Electron app you have open</h3>
      <div class="browx-card-text">
        An Electron app is Chromium, so one started with
        <code>--remote-debugging-port</code> answers the same CDP browxai
        already speaks. Attach to VS Code or Slack and <code>snapshot</code> and
        <code>find</code> read its UI. Attach-only, <code>navigate</code>
        refuses, and whether a port opens at all is up to the app and the
        version.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// host</span>
      <h3>Reaches past the tab</h3>
      <div class="browx-card-text">
        A web task leaves the browser. Switch a capability on and browxai writes
        the real OS clipboard, or pulls a login and a TOTP code from 1Password,
        Bitwarden, LastPass or <code>oathtool</code>. File IO is rooted at one
        workspace directory. All three capabilities start off.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// safety</span>
      <h3>Safe by default</h3>
      <div class="browx-card-text">
        Capability-gated tools, an origin allow and block list, confirmation
        hooks, and a hard anti-wedge deadline on every call. Arbitrary JS, full
        response bodies, the OS clipboard, your password manager, the native
        engines, attaching to a desktop app and network mocking are all off
        until you opt in.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// clients</span>
      <h3>Model-agnostic</h3>
      <div class="browx-card-text">
        Any MCP client over stdio. Use it from Claude, Codex, or anything else
        that speaks the protocol. It is not tied to one model or one vendor.
      </div>
    </div>
    <div class="browx-card">
      <span class="browx-card-mark">// sessions</span>
      <h3>Sessions and lifecycle</h3>
      <div class="browx-card-text">
        Isolated per-session contexts: persistent, incognito, or attach to a
        Chrome you already have open, on your desktop or on a plugged-in Android
        phone. Headed and headless. Pick the engine per session, so one server
        can hold a Chromium page and an emulator app at once. Config is driven
        over MCP, with no out-of-band setup.
      </div>
    </div>
  </div>
</div>

<div class="browx-cta browx-cta--center not-content">
  <a href="/getting-started/" class="browx-btn browx-btn--primary">
    Wire it into your agent
  </a>
  <a href="/security/threat-model/" class="browx-btn browx-btn--ghost">
    Read the threat model
  </a>
</div>
